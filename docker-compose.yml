services:
  cortexthub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cortexthub-backend
    ports:
      - "8003:8001"
    restart: unless-stopped
    env_file:
      - .env